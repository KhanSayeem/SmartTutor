PORT=4000
CLIENT_ORIGIN=http://localhost:5173
JWT_SECRET=replace-with-a-long-random-secret
MONGODB_URI=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=SmartTutor <noreply@smarttutor.local>

# Supabase Storage (free plan). Leave blank to keep the local placeholder
# behaviour -- uploads still succeed, they just return a smarttutor:// URL.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=smarttutor-uploads
